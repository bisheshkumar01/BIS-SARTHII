{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "framework": null,

  "installCommand": "echo 'skipped: the frontend installs inside buildCommand, the function installs from api/requirements.txt'",
  "buildCommand": "[ -d frontend ] && cd frontend; npm ci && npm run build",
  "outputDirectory": "frontend/dist",

  "functions": {
    "api/index.py": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "{backend/**,data/seed/**}"
    }
  },

  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
